services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.61.1-noble
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - STONLY_CONFIG=review
      - PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
